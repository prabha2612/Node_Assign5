doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title display user
        link(rel='stylesheet', href='../styles.css')
    body 
    h1 Enter UserName 
    if users.length
        ul
            each user in users
            li= user.userName
    else
        p No users found